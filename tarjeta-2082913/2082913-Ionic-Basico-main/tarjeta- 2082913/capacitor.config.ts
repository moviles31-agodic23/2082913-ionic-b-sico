import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miApp-1897896',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
